type FullUpdateQuestionPayload {
  id: ID!
  slug: String!
}

extend type Mutation {
  fullUpdateQuestion(
    id: ID!
    title: String!
    tags: [String!]!
    userId: ID!
  ): FullUpdateQuestionPayload
}
