subscription {
  Answer(filter: { mutation_in: [CREATED] }) {
    node {
      node {
        id
        question {
          title
          slug
          user {
            name
            email
          }
        }
        answer {
          content
          user {
            name
            email
          }
        }
      }
    }
  }
}
