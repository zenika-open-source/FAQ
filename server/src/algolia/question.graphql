subscription {
  Question(filter: { mutation_in: [CREATED, UPDATED] }) {
    mutation
    node {
      id
      node {
        id
        question {
          title
          slug
        }
        answer {
          content
        }
        flags {
          type
        }
        tags {
          label
        }
      }
    }
  }
}
