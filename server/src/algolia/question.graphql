subscription {
  Question(filter: { mutation_in: [CREATED, UPDATED] }) {
    mutation
    node {
      id
      node {
        objectID: id
        question {
          title
        }
        answer {
          content
        }
      }
    }
  }
}
