subscription {
  Answer(filter: { mutation_in: [CREATED] }) {
    mutation
    node {
      id
      node {
        id
        question {
          title
          slug
        }
        answer {
          content
        }
        flags {
          type
        }
        tags {
          label
        }
      }
    }
  }
}
