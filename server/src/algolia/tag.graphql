subscription {
  Tag(filter: { mutation_in: [CREATED, DELETED] }) {
    mutation
    node {
      id
      node {
        id
        question {
          title
          slug
        }
        answer {
          content
        }
        flags {
          type
        }
        tags {
          label
        }
      }
    }
  }
}
