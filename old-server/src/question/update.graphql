type FullUpdateQuestionPayload {
  id: ID!
  title: String
  slug: String!
  nodeId: ID!
  tagsChanges: Json!
}

extend type Mutation {
  fullUpdateQuestion(
    id: ID!
    title: String!
    tags: [String!]!
    nodeId: ID!
    userId: ID!
  ): FullUpdateQuestionPayload
}
