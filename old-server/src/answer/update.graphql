type FullUpdateAnswerPayload {
  id: ID!
  content: String
  sourcesChanges: Json!
  nodeId: ID!
}

extend type Mutation {
  fullUpdateAnswer(
    nodeId: ID!
    answerId: ID!
    content: String!
    sources: String!
  ): FullUpdateAnswerPayload
}
