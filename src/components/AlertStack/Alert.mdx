---
name: Alert
route: /components/alert
menu: Components
---

import { Playground, PropsTable } from 'docz'
import { AlertProvider } from './AlertContext.jsx'
import AlertStack from './AlertStack.jsx'
import Button from '../Button'

# Alert

```js
import { AlertStack, AlertProvider } from 'components'
```

## Basic usage

<Playground>
  <AlertProvider>
    <AlertStack />

    <div>
      <b>Push alerts into the stack:</b><br/>
      {
        ['primary', 'info','success','warning','error']
        .map(type => <Button key={type} primary onClick={() => AlertProvider.pushAlert({type, message: 'This is an alert of type: '+type})}>{type}</Button>)
      }
    </div>

  </AlertProvider>
</Playground>
