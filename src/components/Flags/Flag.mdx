---
name: Flag
route: /components/flag
menu: Components
---

import { Playground, PropsTable } from 'docz'
import Flag from './Flag.jsx'
import './Flags.css'

# Flag

```js
import { Flag } from 'components'
```

## Basic usage

<Playground>
  <div className="flags">
    <Flag flag={{ type: 'duplicate' }} />
    <Flag flag={{ type: 'outdated' }} />
    <Flag flag={{ type: 'incomplete' }} />
    <Flag flag={{ type: 'unanswered' }} />
  </div>
  <br />
  <div className="flags">
    <Flag flag={{ type: 'duplicate' }} withlabel={true} />
    <Flag flag={{ type: 'outdated' }} withlabel={true} />
    <Flag flag={{ type: 'incomplete' }} withlabel={true} />
    <Flag flag={{ type: 'unanswered' }} withlabel={true} />
  </div>
</Playground>

## Properties

<PropsTable of={Flag} />
